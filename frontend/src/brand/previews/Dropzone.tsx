import { Dropzone } from "@/brand/components/Dropzone";

export default function DropzonePreview() {
  return <Dropzone title="Upload a scorecard photo" hint="Drag & drop or click to browse" onFile={() => {}} />;
}
